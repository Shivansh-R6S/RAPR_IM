data:
  path: "data/soc-sign-Slashdot081106.txt"
  delimiter: "\t"          # columns are tab-separated

model:
  R: 10                    # number of graph realizations
  seed_sizes: [5, 10, 15, 20, 25]   # k values to evaluate
  mc_simulations: 10       # monte carlo runs per influence estimate (greedy step)

logging:
  level: "INFO"